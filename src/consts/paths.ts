enum Paths { 
    LANDING = '/', 
    FEED = '/feed', 
    PROFILE = '/profile',
    SEARCH = '/search',
    JOIN = '/join', 
    JOININFO = '/joininfo', 
    JOINPROFILEMESSAGE = '/joinprofilemessage',
};

export default Paths;